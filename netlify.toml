[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Ensure environment variable is available in Netlify
[build.environment]
  VITE_RESEND_API_KEY = "${RESEND_API_KEY}"